trigger: none

pool:
  vmImage: 'ubuntu-latest'

resources:
  repositories:
    - repository: pipelines
      type: git
      name: DevPlatform/platform-pipelines
      ref: refs/heads/main

extends:
  template: test/v1/azure-test.yml@pipelines
  parameters:
    build: false
